import type React from "react";
declare type Props = {
    children: React.ReactNode;
    isOpen: boolean;
    onClose: () => void;
};
export declare const ModalWrapper: ({ children, isOpen, onClose }: Props) => JSX.Element;
export {};
