interface Props {
    isOpen: boolean;
    onClose: () => void;
    onConfirm: () => void;
}
export declare const SubmitDataAlert: ({ isOpen, onClose, onConfirm }: Props) => JSX.Element;
export {};
